mixin videoBlock( video = {})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.video__thumbnail(src=video.fileUrl, controls = true)
            h4.video__title=video.title
            h6.video__views=video.views
